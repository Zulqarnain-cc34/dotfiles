# lf — optional legacy file manager

**Primary file manager:** [Yazi](../yazi/README.md)

This config is kept for compatibility. Keybinds and previews may reference external scripts under `~/bin/bash_scripts/`.

To use: `lf` from any terminal.
